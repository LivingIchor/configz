pub const socket_path_fmt = "{s}/configz.sock"; // formatted with XDG_RUNTIME_DIR at runtime
pub const bare_repo_path_fmt = "{s}/.local/share/configz"; // formatted with HOME
pub const log_level = .info;
